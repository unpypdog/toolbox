# 工具箱 (Toolbox)

实用小工具集合，纯静态 HTML 页面，无需后端。

## 项目结构

```
toolbox/
├── index.html              # 工具箱首页
├── pyproject.toml          # 项目配置与依赖
├── uv.lock
├── tools/                  # 各个工具，每个工具一个独立文件夹
│   ├── lung-marker/        # 支气管分段标记
│   ├── rmb-upper/          # 人民币大写转换
│   ├── tax-calc/           # 税点计算器
│   ├── training-cert-batch-fill/  # 操作证书考试账号批量导入
│   └── te-cert-generator/  # TE 培训证书批量生成
└── tests/                  # 测试
```

完整的目录树（含每个文件的作用）和这个工具的**十二条硬约束**见 `CLAUDE.md`。

## 文件组织规则（开发时请遵守）

- 每个工具放在 `tools/<tool-name>/` 下，入口文件命名为 `index.html`
- 测试文件放在 `tests/` 下，命名格式 `test_<tool-name>.py`
- 首页 `index.html` 放在项目根目录
- 新增工具时：在 `tools/` 下创建文件夹 → 编写入口 `index.html` → 在根 `index.html` 添加导航卡片 → 在 `tests/` 创建测试

## 运行测试

```bash
uv sync
uv run python tests/test_toolbox.py
uv run python tests/test_lung_marker.py
uv run python tests/test_rmb_upper.py
uv run python tests/test_tax_calc.py
uv run python tests/test_training_cert_batch_fill.py

# 证书工具（纯 Node，不需要浏览器）
node tests/test_te_cert_cloud_core.js
node tests/lint_te_cert_cloud.js
node tests/test_te_cert_dom_smoke.js
```

## 工具列表

| 工具 | 路径 | 说明 |
|------|------|------|
| 支气管分段标记 | `tools/lung-marker/` | 右下叶基底段顺序标记，支持后悔模式 |
| 税点计算器 | `tools/tax-calc/` | 含税金额 ÷ 税率，一键算税前与税额 |
| 人民币大写转换 | `tools/rmb-upper/` | 数字金额转大写，符合央行规范 |
| 操作证书考试账号批量导入 | `tools/training-cert-batch-fill/` | 本地解析 CSV/Excel，批量新增考试账号 |
| TE 培训证书批量生成 | `tools/te-cert-generator/` | 输入名单即可出证书，PDF 可完全离线生成，也可选云转换 |

### TE 培训证书批量生成

填写形如 `靳睿、耿楠、芮法娟、倪文婧 南京鼓楼医院 25年10月10日` 的名单，
解析成可编辑表格，逐条核对后生成证书。

**只产出 PDF**：证书一旦发出去就是最终版，所以工具**不提供 DOCX 下载** ——
源文件可以被随意改动，不适合交付给学员。DOCX 只作为云转换路径的中间产物存在。

- **PDF**：首选“本地直接生成”，浏览器把模板原图与少量动态文字直接组合成 PDF，
  全程不联网；云转换仍保留作兼容选项。本地模式的动态文字为图像，不能搜索或选中。
  **默认每人一个独立 PDF**（`TE操作培训证书_姓名.pdf`），
  多个文件自动**打包成一个 ZIP** 下载，解压后就是带姓名的单个 PDF。
- **AI 解析名单**（可选）：规则解析只认固定写法。表格粘贴、姓名与信息分行、
  一句话叙述、**手机拍的签到表照片**、以及**图片 + 文字混合**都交给 AI
  （OpenAI 兼容接口，默认 DeepSeek）。

### AI 与本地工作流怎么处理图片和文字

图片可能只有姓名，也可能每一行都有姓名、医院和日期；文字可能补充、修正或分组说明。
两者是**同一次颁发的同一个名单**，不是两批人。这里不再让 AI 自己控制整个流程：

1. **AI 提取图片事实** —— 输出逐行的 `imageRows`，只报告图片中可见的姓名、医院和日期。
2. **AI 提取文字指令** —— 输出 `textPeople` 和 `assignments`；每个赋值只报告字段、值、
   作用范围和原文证据，不决定是否覆盖图片。
3. **本机代码执行合并** —— 固定按「明确姓名 > 分组/行号 > 一一对应顺序 > 唯一全局值」
   的优先级应用文字赋值。这个优先级不受模型措辞和模型版本影响。
4. **本机代码校验冲突** —— 同等范围出现不同值、候选值无法对应、AI 标注存疑时，
   相关行直接进入「需处理」且不会自动勾选。

只有 `ambiguous` 那一种赋值算「存疑」；模型在赋值里附的解释性备注不参与判断，
否则一条**作用于全批**的赋值带个备注，就能把整张表全标成「需处理」、一条都生成不了。

文字里**恰好只有一个值且没有分组迹象**时，它代表用户主动填写的本次全局信息，会整字段
覆盖对应的图片旧值；文字没涉及的图片字段保持原样。多个候选值归属不清时不猜、不覆盖，
保留图片原值并要求人工确认。

所有密钥只存在本机 localStorage；默认不启用 AI。选择“本地直接生成”时证书数据不离开设备；
只有显式选择云转换或 AI 才会联网。

#### PDF 的输出方式

**默认**：每人一个独立 PDF（`TE操作培训证书_姓名.pdf`），多个文件**自动打包成一个 ZIP**
下载，解压后就是一个个带姓名的 PDF（同名重复用 `_2`、`_3` 区分）。
ZIP 是自动的、没有开关 —— 连续触发多次下载会被浏览器拦（要你逐次点「允许」），
还得逐个确认保存位置，这不是需要用户做决定的事。

面板上唯一的开关是「**改为合并成一份多页 PDF**」（默认关）：勾上则合成一本合订本，
适合归档。因为它同时改变交付形态和计费方式，所以留给你决定。

本地直接生成没有 API 调用或费用。云转换的计费差异：逐份 = 每人一次 API 调用；
合并 = 整批一次调用（Adobe 按「1 事务最多 50 页」计费，合并能把 N 次压成 1 次）。

#### 文件命名

**扩展名是固定的**：证书只交付 PDF，所以程序统一追加 `.pdf` / `.zip`，
用户输入的 `.docx` 之类扩展名会被忽略 —— 页面上不出现「选格式」这种误导性选项。

面板里的「**文件命名**」（默认收起）可以改三层的文件名模板，改完即时在下方显示示例：

| 层级 | 默认值 | 可用占位符 |
|---|---|---|
| 每人一个 PDF | `TE操作培训证书_{姓名}` | `{姓名}` `{医院}` `{日期}` `{序号}` |
| 合并成一本 PDF | `TE操作培训证书_{份数}份_{时间}` | `{份数}` `{时间}` |
| 多个 PDF 的压缩包 | `TE操作培训证书_{时间}` | `{份数}` `{时间}` |

- **单行覆盖**：预览表格里的「输出文件名」列可直接编辑，用于个别证书的特殊命名。
  它只在当前批次有效、不跨批次保存；清空即恢复批量规则。
- **去重仍然是自动的**：同名会加 `_2`、`_3`，且预览、上传给云服务的文件名、
  下载下来的文件名**用的是同一个名字**（靠 `record.fileBase`）。
- 全局模板存在本机 localStorage，和密钥分开存；「恢复默认命名」一键还原。


#### 名单解析的三条路

| 方式 | 适用 | 联网 |
|---|---|---|
| 规则解析（默认） | 顿号隔开姓名 + 医院名以「医院/卫生院」等词结尾 + 标准日期 | ❌ 免费离线 |
| AI 解析文本 | 表格粘贴、分行、一句话叙述、含职称工号…… | ✅ 需自备 Key |
| AI 解析图片 | 手写签到表、微信截图等 | ✅ 需自备 Key |

**AI 结果同样要过校验**：缺姓名/缺医院/日期非法的一律红标，绝不静默进生成队列；
模型自己标了存疑的行会额外显示「AI 存疑：…」，并禁止自动勾选。但**请务必对着原图核对姓名**——
证书印错名字不可挽回，校验只能查"缺不缺"，查不出"看错没看错"。


#### 服务商状态

| 服务商 | 状态 | 说明 |
|---|---|---|
| **本地直接生成** | ✅ 已用 Chromium + Word 参照验证 | 完全离线；动态文字为图像，依赖本机微软雅黑 |
| **ConvertAPI** | ✅ 已端到端验证 | 单次 POST、Bearer 头，约 250 次/月 |
| **Adobe PDF Services** | ✅ 已端到端验证 | 500 份/月（额度最宽），但需走 Adobe 的凭据创建流程 |
| CloudConvert | ⚠️ 已实现，未验证 | 下载地址是服务端返回的动态 URL，可能受 CSP 白名单影响 |

#### 用之前先看

- 本地直接生成可直接双击 HTML 使用。云转换或 AI 建议用 http(s) 打开；`file://` 的 origin
  是不透明的 `null`，跨域请求可能被浏览器拦下（报错还很有误导性）。本地起服务：

  ```powershell
  cd <项目根目录>
  python -m http.server 8000
  # 打开 http://127.0.0.1:8000/tools/te-cert-generator/index.html
  ```

- **DOCX 生成不受影响**，`file://` 下照常可用（模板走 base64 载荷）。

修改工具前请先读 `CLAUDE.md` 里这个工具的**十四条硬约束** —— 全部是踩过的坑，
包括模板载荷同步、CSP 白名单、Adobe 专属令牌端点、预检请求头限制、合批分页符、
AI 提示词里不许出现真实日期、「AI 只提取、代码执行合并」这条工作流边界，
以及 AI 的 `max_tokens` / 思考模式 / 截断检查这三件必须一起动的事。


